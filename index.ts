import { taskManager } from "./taskmanager";
import { Task, ProjectTasksManager, PersonalTasksManager, UrgentTasksManager, ProjectTasksTask, PersonalTasksTask, UrgentTasksTask } from "./taskmanager";

// const tasks: Task[] = [new UrgentTasks(), new ProjectTasks(), new PersonalTasks()];

// tasks.forEach((task) => {
//   console.log(task);
// });
